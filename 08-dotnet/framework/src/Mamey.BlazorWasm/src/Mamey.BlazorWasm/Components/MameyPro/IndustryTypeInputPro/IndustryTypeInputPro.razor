@using Mamey.Types
@using Mamey.BlazorWasm.Components.Foundation
@using System.ComponentModel.DataAnnotations
@using Mamey

<MudSelect T="IndustryType?"
           Label="@Label"
           Placeholder="@Placeholder"
           Value="@Value"
           ValueChanged="@ValueChanged"
           Required="@Required"
           Disabled="@Disabled"
           Variant="@Variant"
           Dense="@Dense"
           Class="@Class"
           Style="@Style">
    @if (AllowEmpty && !Required)
    {
        <MudSelectItem T="IndustryType?" Value="@((IndustryType?)null)">-- Select --</MudSelectItem>
    }
    @foreach (var type in Enum.GetValues<IndustryType>())
    {
        var displayName = type.GetEnumDisplayName() ?? type.ToString();
        <MudSelectItem T="IndustryType?" Value="@type">@displayName</MudSelectItem>
    }
</MudSelect>
<FieldMessages HelperText="@HelperText" />

@code {
    [Parameter] public string Label { get; set; } = "Industry Type";
    [Parameter] public string HelperText { get; set; } = "Select the industry type";
    [Parameter] public string Placeholder { get; set; } = "Select industry type";
    [Parameter] public bool Required { get; set; } = false;
    [Parameter] public bool Disabled { get; set; } = false;
    [Parameter] public Variant Variant { get; set; } = Variant.Text;
    [Parameter] public bool Dense { get; set; } = true;
    [Parameter] public bool AllowEmpty { get; set; } = true;
    [Parameter] public string Class { get; set; } = "";
    [Parameter] public string Style { get; set; } = "";

    [Parameter] public IndustryType? Value { get; set; }
    [Parameter] public EventCallback<IndustryType?> ValueChanged { get; set; }
}




