{{- $root := . -}}
{{- range $serviceName, $service := .Values.aiServices }}
{{- if $service.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pupitre-ai-{{ $serviceName }}
  namespace: {{ $root.Values.namespaces.ai }}
  labels:
    {{- include "pupitre.labels" $root | nindent 4 }}
    app.kubernetes.io/component: ai-{{ $serviceName }}
spec:
  replicas: {{ $service.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/component: ai-{{ $serviceName }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: ai-{{ $serviceName }}
    spec:
      serviceAccountName: {{ include "pupitre.serviceAccountName" $root }}
      securityContext:
        {{- toYaml $root.Values.podSecurityContext | nindent 8 }}
      containers:
        - name: ai-{{ $serviceName }}
          securityContext:
            {{- toYaml $root.Values.securityContext | nindent 12 }}
          image: "{{ $root.Values.global.imageRegistry }}/{{ $service.image.repository }}:{{ $service.image.tag }}"
          imagePullPolicy: {{ $service.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ $service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          {{- if $service.resources }}
          resources:
            {{- toYaml $service.resources | nindent 12 }}
          {{- end }}
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://+:{{ $service.port }}"
            - name: AI__DefaultProvider
              value: "AzureOpenAI"
---
apiVersion: v1
kind: Service
metadata:
  name: pupitre-ai-{{ $serviceName }}
  namespace: {{ $root.Values.namespaces.ai }}
  labels:
    {{- include "pupitre.labels" $root | nindent 4 }}
    app.kubernetes.io/component: ai-{{ $serviceName }}
spec:
  type: ClusterIP
  ports:
    - port: {{ $service.port }}
      targetPort: {{ $service.port }}
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: ai-{{ $serviceName }}
{{- end }}
{{- end }}
