# Mamey Government Portal - Environment Variables
# Copy this file to docker.env and fill in the values

# Application Ports
APP_HTTPS_PORT=7295
APP_HTTP_PORT=5075
POSTGRES_PORT=5432

# ASP.NET Core
ASPNETCORE_ENVIRONMENT=Docker

# Certificate Password
CERT_PASSWORD=MameyDev123!


# Authentik OIDC Configuration
# These values should match your Authentik application configuration
# Copy to authentik.env (do not commit secrets)
#
# Generate strong values (per authentik docs):
#   echo "AUTHENTIK_PG_PASS=$(openssl rand -base64 36 | tr -d '\n')" >> authentik.env
#   echo "AUTHENTIK_SECRET_KEY=$(openssl rand -base64 60 | tr -d '\n')" >> authentik.env
#
# See: https://docs.goauthentik.io/install-config/install/docker-compose/

AUTHENTIK_TAG=2025.10.0
AUTHENTIK_AUTHORITY=https://localhost:9444/application/o/mamey-government/
AUTHENTIK_CLIENT_ID=mamey-government-portal
AUTHENTIK_CLIENT_SECRET=your-client-secret-here

# Authentik Service (from docker-compose.authentik.console.yml)
AUTHENTIK_PG_USER=authentik
AUTHENTIK_PG_PASS=your-authentik-db-password
AUTHENTIK_PG_DB=authentik
AUTHENTIK_SECRET_KEY=your-authentik-secret-key
# Exposed ports (we avoid 9000/9001 because MinIO already uses them)
AUTHENTIK_HTTP_PORT=9100
AUTHENTIK_HTTPS_PORT=9444
# Optional
AUTHENTIK_ERROR_REPORTING__ENABLED=false

