{
  "name": "plataforma-principal",
  "version": "1.0.0",
  "description": "Red Soberana API Gateway — Proxy routes to 19 microservices across 35+ service paths",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --forceExit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "http-proxy-middleware": "^3.0.0"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
